/* =============================================================================
   GEOMETRIA DA ASSINATURA — dados gerados por tools/extract.py
   -----------------------------------------------------------------------------
   COMO PREENCHER (30 segundos):

   1. Abra o script.js do projeto isolado da assinatura.
   2. Copie a linha inteira que começa com:

          const SIGNATURE_DATA = {"color":"#D4FB06","bbox":[40.2,74.0, ...

      (é uma linha só, gigante, entre os comentários
       "=== DADOS GERADOS ===" e "=== FIM DOS DADOS GERADOS ===")
   3. Cole no lugar da linha marcada abaixo, substituindo o objeto vazio.
   4. Salve. Não mexa na última linha do arquivo.

   Os dados não foram transcritos automaticamente de propósito: são ~6.000
   coordenadas numéricas e qualquer dígito trocado deforma o traço. Copiar e
   colar o original garante fidelidade byte a byte.
   ============================================================================= */

/* >>> SUBSTITUA A LINHA ABAIXO PELA LINHA COMPLETA DO SCRIPT ORIGINAL <<< */
const SIGNATURE_DATA = { "color": "#D4FB06", "bbox": [40.2, 74.0, 1901.8, 884.0], "strokes": [] };

/* publica no escopo global para que js/assinatura.js encontre os dados */
window.SIGNATURE_DATA = SIGNATURE_DATA;